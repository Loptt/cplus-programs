#include <iostream>
#include <string>
using namespace std;

int p = 13, q = 7;

string encrypt(string msg)
{
    int n = p * q;
}

int main()
{
    string msg;
    cin >> msg;

    cout << encrypt(msg) << endl;
    return 0;
}